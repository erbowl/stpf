%h2 パスワードを忘れたら？
= bootstrap_form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :post }) do |f|
  = devise_error_messages!
  = f.email_field :email, autofocus: true,label:"登録したメールアドレス"
  = f.submit "パスワード新規作成用メールを送信する"
